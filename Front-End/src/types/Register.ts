export interface Register {
    name?: string,
    surname?: string,
    gender?: string,
    birthDate?: Date,
    email?: string,
    password?: string,
    faculty? :string,
    type?: string,
    subjects?: string[],
}